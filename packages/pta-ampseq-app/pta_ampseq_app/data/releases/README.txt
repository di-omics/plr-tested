No combined hardware release manifest is present.

This directory intentionally contains no JSON file. A future release process
may add a reviewed manifest only after the exact combined runner has passed an
attended physical dry run and its final deck state has been reconciled.

The planning server never executes hardware, even when a valid manifest is
added. Execution requires a separate reviewed and tested layer.
